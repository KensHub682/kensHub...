-- KensHub - Animated Private Script - LocalScript
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "KensHub_PrivateScript"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

local tweenInfo = TweenInfo.new(0.4, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

-- Main Background
local bg = Instance.new("Frame")
bg.Name = "MainBackground"
bg.Size = UDim2.new(0.65, 0, 0.78, 0)
bg.Position = UDim2.new(0.175, 0, 0.11, 0)
bg.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
bg.BorderSizePixel = 0
bg.BackgroundTransparency = 1
bg.Visible = true
bg.Parent = screenGui

Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 14)

local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(28, 28, 48)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(12, 12, 25))
}
gradient.Rotation = 90
gradient.Parent = bg

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Name = "TitleBar"
titleBar.Size = UDim2.new(1, 0, 0, 60)
titleBar.BackgroundColor3 = Color3.fromRGB(10, 10, 22)
titleBar.BorderSizePixel = 0
titleBar.Parent = bg

Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 14)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -90, 1, 0)
title.Position = UDim2.new(0, 20, 0, 0)
title.BackgroundTransparency = 1
title.Text = "KensHub | Private Script"
title.TextColor3 = Color3.fromRGB(0, 170, 255)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.Parent = titleBar

local stroke = Instance.new("UIStroke")
stroke.Thickness = 2.5
stroke.Color = Color3.fromRGB(0, 200, 255)
stroke.Transparency = 0.3
stroke.Parent = title

-- Circle Toggle Button
local toggleBtn = Instance.new("TextButton")
toggleBtn.Name = "CircleToggle"
toggleBtn.Size = UDim2.new(0, 46, 0, 46)
toggleBtn.Position = UDim2.new(1, -58, 0, 7)
toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
toggleBtn.Text = "K"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.TextScaled = true
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.Parent = titleBar

Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(1, 0)

local btnStroke = Instance.new("UIStroke")
btnStroke.Thickness = 2.8
btnStroke.Color = Color3.fromRGB(255, 255, 255)
btnStroke.Transparency = 0.5
btnStroke.Parent = toggleBtn

-- Hover Effect
toggleBtn.MouseEnter:Connect(function()
    TweenService:Create(toggleBtn, TweenInfo.new(0.2), {Size = UDim2.new(0, 52, 0, 52)}):Play()
end)
toggleBtn.MouseLeave:Connect(function()
    TweenService:Create(toggleBtn, TweenInfo.new(0.2), {Size = UDim2.new(0, 46, 0, 46)}):Play()
end)

-- ==================== TAB SYSTEM ====================

local tabBar = Instance.new("Frame")
tabBar.Name = "TabBar"
tabBar.Size = UDim2.new(1, 0, 0, 50)
tabBar.Position = UDim2.new(0, 0, 0, 60)
tabBar.BackgroundColor3 = Color3.fromRGB(15, 15, 30)
tabBar.BorderSizePixel = 0
tabBar.Parent = bg

Instance.new("UICorner", tabBar).CornerRadius = UDim.new(0, 0)

local tabContainer = Instance.new("Frame")
tabContainer.Name = "TabContainer"
tabContainer.Size = UDim2.new(1, 0, 1, -110)
tabContainer.Position = UDim2.new(0, 0, 0, 110)
tabContainer.BackgroundTransparency = 1
tabContainer.Parent = bg

-- Tab Frames
local mainTab     = Instance.new("Frame"); mainTab.Name = "MainTab";     mainTab.Size = UDim2.new(1,0,1,0); mainTab.BackgroundTransparency = 1; mainTab.Visible = true;  mainTab.Parent = tabContainer
local proteinTab  = Instance.new("Frame"); proteinTab.Name = "ProteinTab"; proteinTab.Size = UDim2.new(1,0,1,0); proteinTab.BackgroundTransparency = 1; proteinTab.Visible = false; proteinTab.Parent = tabContainer
local miscTab     = Instance.new("Frame"); miscTab.Name = "MiscTab";    miscTab.Size = UDim2.new(1,0,1,0); miscTab.BackgroundTransparency = 1; miscTab.Visible = false; miscTab.Parent = tabContainer
local infoTab     = Instance.new("Frame"); infoTab.Name = "InfoTab";    infoTab.Size = UDim2.new(1,0,1,0); infoTab.BackgroundTransparency = 1; infoTab.Visible = false; infoTab.Parent = tabContainer

-- Tab Buttons
local function createTabButton(text, position, targetTab)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.25, -8, 1, -10)
    btn.Position = UDim2.new(position, 4, 0, 5)
    btn.BackgroundColor3 = Color3.fromRGB(0, 162, 255)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = tabBar

    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 1.5
    stroke.Color = Color3.fromRGB(255, 255, 255)
    stroke.Transparency = 0.7
    stroke.Parent = btn

    btn.MouseButton1Click:Connect(function()
        mainTab.Visible = false
        proteinTab.Visible = false
        miscTab.Visible = false
        infoTab.Visible = false
        targetTab.Visible = true
    end)
    return btn
end

createTabButton("Main",    0,     mainTab)
createTabButton("Protein", 0.25,  proteinTab)
createTabButton("Misc",    0.5,   miscTab)
createTabButton("Info",    0.75,  infoTab)

-- ==================== MAIN TAB ====================

local autoStrength = false
local autoPushup = false
local autoGoldenEgg = false
local autoRebirth = false

local function createToggle(name, yPos, callback)
    local toggle = Instance.new("TextButton")
    toggle.Size = UDim2.new(0.85, 0, 0.09, 0)
    toggle.Position = UDim2.new(0.075, 0, yPos, 0)
    toggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
    toggle.Text = name .. " : OFF"
    toggle.TextColor3 = Color3.fromRGB(255, 100, 100)
    toggle.TextScaled = true
    toggle.Font = Enum.Font.GothamSemibold
    toggle.Parent = mainTab

    Instance.new("UICorner", toggle).CornerRadius = UDim.new(0, 12)

    toggle.MouseButton1Click:Connect(function()
        callback(toggle)
    end)
    return toggle
end

createToggle("Auto Strength", 0.08, function(btn)
    autoStrength = not autoStrength
    if autoStrength then
        btn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
        btn.Text = "Auto Strength : ON"
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        task.spawn(function()
            while autoStrength do
                pcall(function() ReplicatedStorage:WaitForChild("addStrength"):FireServer() end)
                task.wait(0.08)
            end
        end)
    else
        btn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        btn.Text = "Auto Strength : OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

createToggle("Auto Pushup", 0.20, function(btn)
    autoPushup = not autoPushup
    if autoPushup then
        btn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
        btn.Text = "Auto Pushup : ON"
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        task.spawn(function()
            while autoPushup do
                pcall(function() ReplicatedStorage:WaitForChild("ShieldTrainingEvent"):FireServer() end)
                task.wait(0.12)
            end
        end)
    else
        btn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        btn.Text = "Auto Pushup : OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

createToggle("Auto Open Golden Egg", 0.32, function(btn)
    autoGoldenEgg = not autoGoldenEgg
    if autoGoldenEgg then
        btn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
        btn.Text = "Auto Open Golden Egg : ON"
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        task.spawn(function()
            while autoGoldenEgg do
                pcall(function() ReplicatedStorage:WaitForChild("PetEggEvent994"):FireServer("OpenEgg") end)
                task.wait(0.7)
            end
        end)
    else
        btn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        btn.Text = "Auto Open Golden Egg : OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

createToggle("Auto Rebirth", 0.44, function(btn)
    autoRebirth = not autoRebirth
    if autoRebirth then
        btn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
        btn.Text = "Auto Rebirth : ON"
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        task.spawn(function()
            while autoRebirth do
                pcall(function()
                    local rEvents = ReplicatedStorage:WaitForChild("rEvents")
                    rEvents:WaitForChild("rebirthEvent"):FireServer()
                end)
                task.wait(1.4)
            end
        end)
    else
        btn.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        btn.Text = "Auto Rebirth : OFF"
        btn.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

-- ==================== PROTEIN TAB ====================

local function createBoostButton(name, price, args, yPos)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.85, 0, 0.105, 0)
    btn.Position = UDim2.new(0.075, 0, yPos, 0)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 55)
    btn.Text = name .. "  " .. price .. "🔮"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = proteinTab

    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)

    local boostStroke = Instance.new("UIStroke")
    boostStroke.Thickness = 1.5
    boostStroke.Color = Color3.fromRGB(0, 170, 255)
    boostStroke.Parent = btn

    btn.MouseButton1Click:Connect(function()
        pcall(function()
            ReplicatedStorage:WaitForChild("BuyBoostEvent"):FireServer(unpack(args))
        end)
        btn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
        task.delay(0.4, function()
            btn.BackgroundColor3 = Color3.fromRGB(35, 35, 55)
        end)
    end)
end

createBoostButton("1.8x Strength - Atom Crunch", "4500", {"buy", "Atom Crunch", 1}, 0.08)
createBoostButton("1.25x Shield - Chocolate Bar", "1500", {"buy", "Chocolate Bar 2", 1}, 0.22)
createBoostButton("1.2x Shield - Chocolate Bar", "1250", {"buy", "Chocolate Bar 1", 1}, 0.36)

-- ==================== MISC & INFO ====================

-- Anti AFK
local antiAFKEnabled = false
local antiAFKConnection

local antiAFKButton = Instance.new("TextButton")
antiAFKButton.Size = UDim2.new(0.6, 0, 0.12, 0)
antiAFKButton.Position = UDim2.new(0.2, 0, 0.15, 0)
antiAFKButton.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
antiAFKButton.Text = "Anti AFK : OFF"
antiAFKButton.TextColor3 = Color3.fromRGB(255, 100, 100)
antiAFKButton.TextScaled = true
antiAFKButton.Font = Enum.Font.GothamSemibold
antiAFKButton.Parent = miscTab

Instance.new("UICorner", antiAFKButton).CornerRadius = UDim.new(0, 12)

antiAFKButton.MouseButton1Click:Connect(function()
    antiAFKEnabled = not antiAFKEnabled
    if antiAFKEnabled then
        antiAFKButton.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
        antiAFKButton.Text = "Anti AFK : ON"
        antiAFKButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        
        local vu = game:GetService("VirtualUser")
        antiAFKConnection = player.Idled:Connect(function()
            vu:CaptureController()
            vu:ClickButton2(Vector2.new())
        end)
    else
        antiAFKButton.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        antiAFKButton.Text = "Anti AFK : OFF"
        antiAFKButton.TextColor3 = Color3.fromRGB(255, 100, 100)
        if antiAFKConnection then antiAFKConnection:Disconnect() end
    end
end)

-- Info Tab
local infoText = Instance.new("TextLabel")
infoText.Size = UDim2.new(0.9, 0, 0.75, 0)
infoText.Position = UDim2.new(0.05, 0, 0.12, 0)
infoText.BackgroundTransparency = 1
infoText.Text = "Welcome To KensHub Private Script!\n\n• Fast & Clean UI\n• Working Auto Farm\n• Boosts & More\n\nJoin our Discord for updates & more scripts!"
infoText.TextColor3 = Color3.fromRGB(200, 220, 255)
infoText.TextScaled = true
infoText.TextWrapped = true
infoText.Font = Enum.Font.GothamMedium
infoText.TextYAlignment = Enum.TextYAlignment.Top
infoText.Parent = infoTab

-- ==================== TOGGLE & DRAGGING ====================

local isVisible = true
local dragging = false
local dragStart
local startPos

local function playIntroAnimation()
    bg.Size = UDim2.new(0.3, 0, 0.3, 0)
    bg.Position = UDim2.new(0.35, 0, 0.35, 0)
    bg.BackgroundTransparency = 1
    TweenService:Create(bg, tweenInfo, {Size = UDim2.new(0.65, 0, 0.78, 0)}):Play()
    TweenService:Create(bg, tweenInfo, {Position = UDim2.new(0.175, 0, 0.11, 0)}):Play()
    TweenService:Create(bg, tweenInfo, {BackgroundTransparency = 0}):Play()
end

local function toggleGUI()
    isVisible = not isVisible
    if isVisible then
        bg.Visible = true
        toggleBtn.Parent = titleBar
        toggleBtn.Position = UDim2.new(1, -58, 0, 7)
        TweenService:Create(bg, tweenInfo, {BackgroundTransparency = 0}):Play()
        TweenService:Create(bg, tweenInfo, {Size = UDim2.new(0.65, 0, 0.78, 0)}):Play()
        TweenService:Create(bg, tweenInfo, {Position = UDim2.new(0.175, 0, 0.11, 0)}):Play()
    else
        local hideTween = TweenService:Create(bg, tweenInfo, {
            BackgroundTransparency = 1,
            Size = UDim2.new(0.3, 0, 0.3, 0),
            Position = UDim2.new(0.35, 0, 0.35, 0)
        })
        hideTween:Play()
        hideTween.Completed:Once(function()
            bg.Visible = false
        end)
        
        toggleBtn.Parent = screenGui
        TweenService:Create(toggleBtn, TweenInfo.new(0.35, Enum.EasingStyle.Quint), {
            Position = UDim2.new(0.5, -23, 0.5, -23)
        }):Play()
    end
end

toggleBtn.MouseButton1Click:Connect(toggleGUI)

-- Dragging
local function startDragging(obj)
    obj.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = obj.Position
        end
    end)
end

startDragging(titleBar)
startDragging(toggleBtn)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        local target = isVisible and bg or toggleBtn
        target.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

playIntroAnimation()
print("✅ KensHub Private Script Loaded Successfully!")
